export enum StorageKey { Theme = "theme" }
export enum SessionKey { ArticleBackPath = "article-back-path" }
export enum PageKind { Home = "home", Articles = "articles", Article = "article", Tags = "tags", InfoFile = "info-file", NotFound = "not-found" }
export enum ViewMode { List = "list", Article = "article", Error = "error" }
export enum PanelId { Home = "home", Articles = "articles", Tags = "tags" }
export enum CssClass { Hidden = "hidden", Active = "is-active", ZenMode = "zen-mode", ZenTopHover = "zen-top-hover", NotFoundMode = "not-found-mode" }
export enum DebounceDelay { Search = 120 }
export enum CacheLimit { ArticleHtml = 20, ArticleMeta = 80, ArticleStats = 80 }
